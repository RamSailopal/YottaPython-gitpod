
tasks:
  - init: |
      echo 'TODO: build project'
    command: |
      cd data && docker run --name yottadb -it -v $PWD:/data ramb0/yottadbjupyter /opt/yottadb/current/ydb

vscode:
  extensions:
    - formulahendry.code-runner
    - ms-python.python
